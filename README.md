## Ionic Template but still a disappointment

First things first, load package.json.

Then npm install. 

Then make sure your jasmine is working. 

'''npm install jasmine'''

If that does not work, then 

'''npm install --save-dev @types/jasmine'''
